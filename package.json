{
  "name": "battle-formation",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/mobile",
    "android": "npm run android --workspace=apps/mobile",
    "ios": "npm run ios --workspace=apps/mobile",
    "web": "npm run web --workspace=apps/mobile",
    "build:game": "npm run build --workspace=@battle-formation/game-engine && node scripts/embed-game-bundle.mjs",
    "build:node-libs": "npm run build --workspace=@battle-formation/shared-types && npm run build:node --workspace=@battle-formation/game-engine",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "postinstall": "npm run build:node-libs && ( [ \"$SKIP_GAME_BUILD\" = \"true\" ] || npm run build:game )"
  },
  "engines": {
    "node": ">=20"
  }
}
